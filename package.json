{
  "name": "label-by-zare-storefront",
  "private": true,
  "version": "29.0.0",
  "scripts": {
    "build": "node scripts/optimize-images.js --build && node scripts/build-site.js",
    "deploy": "wrangler deploy",
    "test": "node scripts/check-syntax.js && node scripts/build-templates.js && node --test tests/*.test.js",
    "check": "npm run test && npm run build",
    "images": "node scripts/optimize-images.js"
  },
  "devDependencies": {
    "sharp": "0.35.4",
    "wrangler": "4.129.1"
  }
}
